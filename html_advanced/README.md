This project contains a basic HTML file named 0-index.html.

The file includes:

The HTML5 doctype declaration
An <html> tag with language set to English (lang="en")
Text direction set to left-to-right (dir="ltr")

The page is intentionally empty and displays a blank screen when opened in a browser.

This task demonstrates the basic structure of an HTML document.
